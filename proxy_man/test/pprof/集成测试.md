# 测试上传http
```bash
# ContentLength > 0 场景（应该保持低内存）
tcurl -X POST -p -x http://127.0.0.1:8080 --data-binary @test/data/huge_100m.bin http://localhost:9001/test/upload

# Chunked 编码场景（应该使用磁盘，内存仍低）
tcurl -X POST  -p -x http://127.0.0.1:8080 -H "Transfer-Encoding: chunked"  --data-binary @test/data/huge_100m.bin http://localhost:9001/test/upload
```
# 测试上传https
```bash
# ContentLength > 0 场景（应该保持低内存）
tcurl -X POST -k -x http://127.0.0.1:8080 --data-binary @test/data/huge_100m.bin https://localhost:9002/test/upload

# Chunked 编码场景（应该使用磁盘，内存仍低）
tcurl -X POST -k -x http://127.0.0.1:8080 -H "Transfer-Encoding: chunked"  --data-binary @test/data/huge_100m.bin https://localhost:9002/test/upload
```




